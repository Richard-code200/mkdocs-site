# Zed 快捷键

本文主要记录当前本机 `~/.config/zed/keymap.json` 中未注释的自定义配置项；为避免误解，也会标注 `null` 禁用项、相关注释项和存在识别疑问的配置。当前 `~/.config/zed/settings.json` 使用 VSCode 基础键位，启用 Vim 模式，并通过 `vim.toggle_relative_line_numbers: true` 在普通模式显示相对行号、在插入模式显示绝对行号；本文未列出的 VSCode、Vim 或 Zed 默认快捷键仍可能有效。

## 按键约定

- `Space` 是 Leader 键，例如 `Space g s` 表示依次按下三个键，而不是同时按下。
- `Ctrl`、`Shift` 是修饰键；`Ctrl-Shift-s` 表示同时按下这三个键。
- 空格分隔的按键是连续按键，例如 `g r`、`Ctrl-x Ctrl-o`。
- 大写单键表示配合 `Shift`，例如 `A` 等同于 `Shift-a`，`S` 等同于 `Shift-s`。
- 同一个键可因当前焦点、Vim 模式、菜单状态而执行不同操作；应结合各表的“上下文”列判断。
- 表中的 `|` 表示竖线键，`\` 表示反斜杠键。

## 高频速查

| 快捷键 | 功能 | 主要上下文 |
| --- | --- | --- |
| `Ctrl-h/j/k/l` | 切换到左/下/上/右窗格 | 全局 |
| `Ctrl-\` | 切换底部 Dock，通常用于终端 | 全局 |
| `Ctrl-s` | 保存文件 | Vim 控制状态且无等待/菜单 |
| `Space e` / `\` | 在项目面板定位当前文件，或切换项目面板 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |
| `Space s f` | 文件搜索 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |
| `Space s g` | 全局文本搜索 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |
| `Space Space` | 大纲搜索 | Vim 控制状态且无等待/菜单 |
| `Space s s` | 项目符号搜索 | Vim 控制状态且无等待/菜单 |
| `Space o` | 打开或关闭标签切换器 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView |
| `Space c a` | 打开 Code Actions | Vim 控制状态且无等待/菜单 |
| `Space r n` | 重命名符号 | Vim 控制状态且无等待/菜单 |
| `Space g p` | 切换 Git 面板焦点 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |
| `Space a a` | 切换 Agent 面板焦点 | Vim 控制状态且无等待/菜单；`AgentPanel > Markdown` 中改为切换右侧 Dock |
| `Space a i` | 打开 Inline Assist | Vim 控制状态且无等待/菜单 |

## 全局、窗格与标签

以下绑定没有限定上下文，因此也可能覆盖编辑器、终端或面板原本对同键的处理。

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl-h` | 激活左侧窗格 |
| `Ctrl-j` | 激活下方窗格 |
| `Ctrl-k` | 激活上方窗格 |
| `Ctrl-l` | 激活右侧窗格 |
| `Ctrl-\` | 切换底部 Dock |

以下操作仅在 `Editor && VimControl && !VimWaiting && !menu` 中生效，即编辑器处于 Vim 控制状态、没有等待后续 Vim 按键且菜单未打开。

| 快捷键 | 功能 |
| --- | --- |
| <code>Space &#124;</code> | 向右分屏 |
| `Space _` | 向下分屏 |
| `Space x w` | 关闭当前标签 |
| `Space o` | 打开或关闭标签切换器 |
| `Shift-h` | 切换到上一个标签 |
| `Shift-l` | 切换到下一个标签 |
| `Shift-q` | 关闭当前标签 |

## 保存、定位与搜索

### 文件与项目

| 快捷键 | 功能 | 上下文 |
| --- | --- | --- |
| `Ctrl-s` | 保存文件 | Vim 控制状态且无等待/菜单 |
| `Ctrl-Shift-s` | 保存但不格式化 | Vim 控制状态且无等待/菜单 |
| `Space e` | 在项目面板中定位当前文件 | Vim 控制状态且无等待/菜单 |
| `\` | 在项目面板中定位当前文件 | Vim 控制状态且无等待/菜单 |
| `Space s p` | 在当前窗口打开最近项目（`create_new_window: false`） | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |

### 标签与搜索

| 快捷键 | 功能 | 上下文 |
| --- | --- | --- |
| `Space s f` | 打开文件搜索器 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |
| `Space s g` | 展开全局文本搜索 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |
| `Space Space` | 打开大纲搜索 | Vim 控制状态且无等待/菜单 |
| `Space s s` | 打开项目符号搜索 | Vim 控制状态且无等待/菜单 |
| `Space o` | 打开或关闭标签切换器 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView |
| `Shift-h` | 上一个标签 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView |
| `Shift-l` | 下一个标签 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView |

## LSP 与诊断

| 快捷键 | 功能 | 上下文 |
| --- | --- | --- |
| `Shift-k` | 显示 Hover 信息 | Vim 控制状态且无等待/菜单 |
| `Space f` | 格式化文件 | Vim 控制状态且无等待/菜单 |
| `Space c a` | 打开或关闭 Code Actions | Vim 控制状态且无等待/菜单 |
| `Space r n` | 重命名符号 | Vim 控制状态且无等待/菜单 |
| `g r` | 查找全部引用 | Vim 控制状态且无等待/菜单 |
| `] d` | 跳到下一个诊断 | Vim 控制状态且无等待/菜单 |
| `[ d` | 跳到上一个诊断 | Vim 控制状态且无等待/菜单 |
| `c a` | 在 `c` 操作符等待状态中打开 Code Actions | `Editor && vim_operator == c` |
| `c c` | 在 `c` 操作符等待状态中选择当前行 | `Editor && vim_operator == c` |
| `Ctrl-s` | 显示函数签名帮助 | Vim 插入模式 |

## Git 与外部任务

### Git 操作

| 快捷键 | 功能 | 上下文 |
| --- | --- | --- |
| `] h` | 跳到下一个 Git hunk | Vim 控制状态且无等待/菜单 |
| `[ h` | 跳到上一个 Git hunk | Vim 控制状态且无等待/菜单 |
| `Space g s` | 切换当前改动的暂存状态 | Vim 控制状态且无等待/菜单 |
| `Space g d` | 打开当前改动的 Diff | Vim 控制状态且无等待/菜单 |
| `Space g r` | 恢复当前改动 | Vim 控制状态且无等待/菜单 |
| `Space g p` | 切换 Git 面板焦点 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |

### 外部任务

| 快捷键 | 任务 | 显示位置 | 可用上下文 |
| --- | --- | --- | --- |
| `Space g g` | 启动名为 `lazygit` 的任务 | 中央区域 | Vim 控制状态且无等待/菜单，或 EmptyPane、SharedScreen、KeyContextView、ProjectPanel（非编辑状态） |
| `Space g h` | 启动名为 `dash` 的任务 | 中央区域 | 仅 Vim 控制状态且无等待/菜单 |

这两个绑定只调用 Zed 的 `task::Spawn`，因此依赖已经定义的同名 Zed 任务。实际执行的命令及其依赖由任务定义决定，当前快捷键配置本身无法证明这些细节。

## AI 与 Edit Prediction

| 快捷键 | 功能 | 上下文 |
| --- | --- | --- |
| `Space a a` | 切换 Agent 面板焦点 | Vim 控制状态且无等待/菜单 |
| `Space a i` | 打开 Inline Assist | Vim 控制状态且无等待/菜单 |
| `Ctrl-c` | 取消 Inline Assist | `InlineAssistEditor` |
| `Space a a` | 切换右侧 Dock | `AgentPanel > Markdown` |
| `Escape` | 切换右侧 Dock | Agent 面板或 Agent 的消息编辑器 |
| `Ctrl-x Ctrl-c` | 显示 Edit Prediction | Vim 插入模式 |
| `Shift-enter` | 接受 Edit Prediction | 存在预测且未显示补全菜单 |
| `Ctrl-enter` | 接受 Edit Prediction；后定义绑定优先于较早的 Vim 控制状态 `null` | `!ContextEditor > (Editor && mode == full)`；更晚定义的补全和 Code Actions 确认绑定会覆盖它 |
| `Ctrl-enter` | 以较早的 `null` 阻止无法处理的预测动作回落到默认 Inline Assist，并非禁用上述后定义动作 | Vim 控制状态且无等待/菜单 |
| `Ctrl-enter` | 禁用该键，绑定值为 `null` | Terminal |

`settings.json` 将 Edit Prediction provider 配置为 Zed，并使用 `subtle` 模式。Agent 当前配置在左侧 Dock，而上述 Agent 上下文动作是 `workspace::ToggleRightDock`；两者可能不一致，因此不能保证这些动作会关闭 Agent 面板。

## 面板与特殊视图

### ProjectPanel

以下绑定在项目面板获得焦点且不处于重命名等编辑状态时生效。

| 快捷键 | 功能 |
| --- | --- |
| `a` | 新建文件 |
| `A` | 新建目录 |
| `r` | 重命名 |
| `d` | 删除 |
| `x` | 剪切 |
| `c` | 复制 |
| `p` | 粘贴 |
| `z` | 折叠选中条目 |
| `Z` | 折叠全部条目 |
| `q` | 切换左侧 Dock，关闭项目面板 |
| `\` | 切换左侧 Dock，关闭项目面板 |
| `Space e` | 切换左侧 Dock，关闭项目面板 |
| `Escape` | 切换左侧 Dock，关闭项目面板 |
| `Shift-g` | 选择最后一项 |
| `g g` | 选择第一项 |
| `Space s f` | 打开文件搜索器 |
| `Space s g` | 展开全局文本搜索 |
| `Space s p` | 在当前窗口打开最近项目 |
| `Space g p` | 切换 Git 面板焦点 |
| `Space g g` | 在中央区域启动 `lazygit` 任务 |

### GitPanel 与 ProjectSearchBar

| 快捷键 | 功能 | 上下文 |
| --- | --- | --- |
| `q` | 切换左侧 Dock | Git 面板且焦点不在提交编辑器 |
| `Escape` | 切换左侧 Dock | Git 面板；无论是否在提交编辑器均有对应绑定 |
| `Ctrl-c` | 关闭当前搜索项 | `ProjectSearchBar > Editor` |
| `Escape` | 关闭当前搜索项 | `ProjectSearchBar > Editor` |

### EmptyPane、SharedScreen 与 KeyContextView

| 快捷键 | 功能 | 上下文 |
| --- | --- | --- |
| `Space s f` | 打开文件搜索器 | 三种上下文 |
| `Space s g` | 展开全局文本搜索 | 三种上下文 |
| `Space s p` | 在当前窗口打开最近项目 | 三种上下文 |
| `Space o` | 打开或关闭标签切换器 | 三种上下文 |
| `Shift-h` | 上一个标签 | 三种上下文 |
| `Shift-l` | 下一个标签 | 三种上下文 |
| `Space e` | 切换项目面板焦点 | 三种上下文 |
| `\` | 切换项目面板焦点 | 三种上下文 |
| `Space g p` | 切换 Git 面板焦点 | 三种上下文 |
| `Space g g` | 在中央区域启动 `lazygit` 任务 | 三种上下文 |
| `Shift-q` | 关闭当前标签 | 仅 SharedScreen 与 KeyContextView |

`EmptyPane` 中的 `Shift-q` 配置已被注释，因此没有自定义关闭绑定。

### MarkdownPreview

| 快捷键 | 功能 |
| --- | --- |
| `Shift-q` | 关闭当前 Markdown 预览标签 |

MarkdownPreview 不属于上表的“三种上下文”组合，因此没有从该组合获得 `Space s f` 等绑定。

### Terminal

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl-Shift-h` | 上一个终端标签 |
| `Ctrl-Shift-l` | 下一个终端标签 |
| `Ctrl-Shift-q` | 关闭当前终端标签 |
| `Ctrl-Shift-x` | 关闭当前终端标签 |
| `Ctrl-Shift-o` | 新建终端 |
| `Ctrl-Shift-r` | 重新运行任务 |
| `Ctrl-enter` | 禁用该键，绑定值为 `null` |

## Vim 普通与可视模式

### 显示行、滚动与搜索居中

在 Vim 控制状态且无等待/菜单时，`j`、`k` 按显示行移动，适合软换行文本。滚动和搜索命令通过发送连续按键，在动作结束后追加 `z z` 居中当前行。

| 快捷键 | 功能 |
| --- | --- |
| `j` | 向下移动一个显示行 |
| `k` | 向上移动一个显示行 |
| `Ctrl-Shift-u` | 执行原始向上滚动 |
| `Ctrl-Shift-d` | 执行原始向下滚动 |
| `Ctrl-u` | 向上滚动后执行 `z z` 居中 |
| `Ctrl-d` | 向下滚动后执行 `z z` 居中 |
| `n` | 跳到下一个搜索结果后居中 |
| `Shift-n` | 跳到上一个搜索结果后居中 |
| `Shift-g` | 跳到文件末尾后居中 |

### Sneak、Surround 与文本对象

| 快捷键 | 功能 | 模式或上下文 |
| --- | --- | --- |
| `s` | 启动向前 Sneak | 普通或可视模式 |
| `S` | 启动向后 Sneak | 普通或可视模式 |
| `g s a` | 添加 Surround | 普通或可视模式，未等待其他 Vim 输入 |
| `g s d` | 删除 Surround | 普通或可视模式，未等待其他 Vim 输入 |
| `g s r` | 修改 Surround | 普通或可视模式，未等待其他 Vim 输入 |
| `b` | mini 风格括号文本对象 | `a`、`i`、`cs` 或 `sa` 操作符等待状态 |
| `q` | mini 风格引号文本对象 | `a`、`i`、`cs` 或 `sa` 操作符等待状态 |

### 系统剪贴板

这些映射向 Vim 发送 `"+` 寄存器序列。

| 快捷键 | 功能 | 模式 |
| --- | --- | --- |
| `g y` | 使用系统剪贴板寄存器复制 | 普通或可视模式 |
| `g p` | 从系统剪贴板在光标后粘贴 | 普通模式 |
| `g P` | 从系统剪贴板在光标前粘贴 | 普通模式 |
| `g p` | 从系统剪贴板替换选区 | 可视模式 |
| `g P` | 从系统剪贴板替换选区；实际同样发送小写 `p` | 可视模式 |

### 可视模式编辑

| 快捷键 | 功能 |
| --- | --- |
| `Shift-j` | 下移选中的行 |
| `Shift-k` | 上移选中的行 |
| `>` | 增加缩进并通过 `g v` 保持选区 |
| `<` | 减少缩进并通过 `g v` 保持选区 |

## Vim 插入模式

### 退出与编辑

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl-c` | 以 `NormalBefore` 方式返回普通模式 |
| `Ctrl-[` | 以 `NormalBefore` 方式返回普通模式 |
| `Escape` | 以 `NormalBefore` 方式返回普通模式 |
| `Ctrl-w` | 删除到前一个单词开头 |
| `Ctrl-u` | 删除到行首 |
| `Ctrl-t` | 增加缩进 |
| `Ctrl-d` | 减少缩进 |
| `Ctrl-k` | 输入 Digraph |
| `Ctrl-v` | 输入字面字符 |
| `Ctrl-q` | 输入字面字符 |
| `Ctrl-Shift-q` | 输入字面字符 |
| `Ctrl-Shift-v` | 从系统剪贴板粘贴 |
| `Ctrl-r` | 选择并插入寄存器内容 |
| `Insert` | 切换替换模式 |
| `Ctrl-o` | 临时执行一次普通模式命令 |
| `Ctrl-s` | 显示签名帮助 |

### Ctrl-x 序列

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl-x` | 单独按下时禁用，绑定值为 `null` |
| `Ctrl-x Ctrl-o` | 显示补全菜单 |
| `Ctrl-x Ctrl-a` | 禁用，绑定值为 `null`；不会启动 Inline Assist |
| `Ctrl-x Ctrl-c` | 显示 Edit Prediction |
| `Ctrl-x Ctrl-l` | 打开或关闭 Code Actions |
| `Ctrl-x Ctrl-z` | 取消当前编辑器交互 |

`j j` 与 `j k` 退出插入模式的整个配置块已被注释，因此两组按键当前均**不生效**。

## 菜单与预测覆盖

以下上下文对常见确认键设置了自定义覆盖。

| 上下文 | `Tab` | `Enter` | `Ctrl-enter` | `Shift-enter` | `Ctrl-y` |
| --- | --- | --- | --- | --- | --- |
| 显示补全菜单 | 插入 Tab | 插入换行 | 确认补全 | 确认补全 | 确认补全 |
| 显示 Code Actions | 插入 Tab | 插入换行 | 确认 Code Action | 确认 Code Action | 确认 Code Action |
| 存在 Edit Prediction 且未显示补全 | 插入 Tab | 插入换行 | 由完整编辑器绑定接受预测；专用上下文绑定虽已注释，但不会取消该绑定 | 接受预测 | 此上下文内没有生效绑定 |
| 正在重命名 | 没有自定义覆盖 | 确认重命名 | 配置意图为确认重命名；键名写作 `ctrl+enter`，是否识别需实测 | 没有自定义覆盖 | 没有自定义覆盖 |

同一上下文树层级发生冲突时，后定义的绑定获胜，`null` 也遵守这一优先级。完整 Editor 且不在 ContextEditor 时，后定义的 `editor::AcceptEditPrediction` 因而优先于较早的 Vim 控制状态 `null`；如果该动作不能处理按键，较低优先级的 `null` 会阻止继续回落到默认 Inline Assist。更晚定义的补全菜单和 Code Actions 确认绑定则会覆盖预测动作。

Edit Prediction 专用上下文里的 `Ctrl-enter` 和 `Ctrl-y` 两项均已被注释；这只表示专用绑定未生效，不会取消较宽泛的完整编辑器 `Ctrl-enter` 绑定。重命名配置使用官方语法未记录的 `ctrl+enter`，因此这里只记录其确认重命名的配置意图，实际识别仍需测试。

## 上下文差异与注意事项

| 按键 | 差异或风险 |
| --- | --- |
| `Ctrl-s` | Vim 控制状态且无等待/菜单时保存文件；Vim 插入模式中显示签名帮助。 |
| `Shift-k` | Vim 控制状态且无等待/菜单时显示 Hover；可视模式专用上下文中上移选中的行。 |
| `Space e` / `\` | Vim 控制状态且无等待/菜单时在项目面板定位当前文件；EmptyPane、SharedScreen、KeyContextView 中切换项目面板焦点；ProjectPanel 非编辑状态中关闭左侧 Dock。 |
| `Ctrl-h/j/k/l` | 是无上下文限制的全局窗格导航，可能优先于编辑器、终端或面板自己的同键行为。 |
| `Ctrl-enter` | 完整 Editor 且不在 ContextEditor 时接受 Edit Prediction，优先于较早的 Vim 控制状态 `null`；动作无法处理时，该 `null` 阻止回落到默认 Inline Assist。更晚的补全与 Code Actions 确认绑定会覆盖预测动作；Terminal 中保持禁用。 |
| `ctrl+enter` | 重命名配置使用加号写法 `ctrl+enter`，不同于其余位置的 `ctrl-enter`；本文忠实记录意图，但是否被当前 Zed 版本识别需以实际按键测试为准。 |
| `j j` / `j k` | 退出插入模式的配置块已整体注释，当前不生效。 |

此外，`Editor && VimControl` 中的 `Ctrl-c` 会发送 `Escape`；插入模式与 Inline Assist 又分别将它映射为返回普通模式和取消辅助编辑。具体执行哪个动作由最匹配的上下文决定。

## 配置维护

- 自定义键位来源：`~/.config/zed/keymap.json`。
- 基础键位、Vim 开关和普通/插入模式相对行号切换来源：`~/.config/zed/settings.json`，其中 `vim.toggle_relative_line_numbers: true`。
- 修改配置后，应重新逐项检查上下文条件、空绑定 `null` 和被注释的代码，避免把禁用项误写成有效快捷键。
- 在命令面板运行 `zed: open default keymap` 可查看当前 Zed 版本的默认键位，并与自定义覆盖进行对照。
- 外部任务快捷键失效时，应同时检查 Zed 任务定义、任务名称以及任务定义声明的实际命令和依赖，而不应只检查 keymap。
